# Generates a genes main table

Generates a genes main table

## Usage

``` r
ingest_gene_data(dir_data)
```

## Arguments

- dir_data:

  String. The directory in which you downloaded the data

## Value

A data.table with the genes to gene information (Ensembl identifiers,
symbols and biotype)
