# Check GeneWalk graph data.table

Checkmate extension for checking the graph_dt parameter of a GeneWalk
object.

## Usage

``` r
checkGeneWalkGraphDt(x)
```

## Arguments

- x:

  The data.table to check.

## Value

`TRUE` if the check was successful, otherwise an error message.
