# Generates a Reactome term ontology table

Generates a Reactome term ontology table

## Usage

``` r
ingest_reactome_ontology(dir_data)
```

## Arguments

- dir_data:

  String. The directory in which you downloaded the data

## Value

A data.table with the Reactome ontology (child of, etc.)
