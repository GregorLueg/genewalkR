# Generates a Gene Ontology to gene table

Generates a Gene Ontology to gene table

## Usage

``` r
ingest_gene_ontology_genes(dir_data)
```

## Arguments

- dir_data:

  String. The directory in which you downloaded the data

## Value

A data.table with the Genes to Gene Ontology term associations
