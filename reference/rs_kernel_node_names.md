# Pull out the node names from the kernel

Pull out the node names from the kernel

## Usage

``` r
rs_kernel_node_names(kernel)
```

## Arguments

- kernel:

  External pointer to the `DiffusionKernel`.

## Value

A string vector from R.
