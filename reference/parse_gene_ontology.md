# Parses the Gene Ontology

Parses the Gene Ontology

## Usage

``` r
parse_gene_ontology(dir_data)
```

## Arguments

- dir_data:

  String. The directory in which you downloaded the data

## Value

A named list with the different sub tables and data extracted from the
.obo file
