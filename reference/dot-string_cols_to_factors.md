# Transform string to factor columns

Transform string to factor columns

## Usage

``` r
.string_cols_to_factors(table)
```

## Arguments

- table:

  data.table. The data.table for which to transform the factor columns
  into strings.

## Value

The data.table with the factor columns transformed to strings.
