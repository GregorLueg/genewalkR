% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diffusion_profiles.R
\name{generate_profiles}
\alias{generate_profiles}
\title{Generate diffusion profiles}
\usage{
generate_profiles(
  object,
  seeds,
  diffusion_profile_params = params_diffusion_profiles(),
  .verbose = TRUE
)
}
\arguments{
\item{object}{A \code{DiffusionProfiles} object, see \code{\link[=DiffusionProfiles]{DiffusionProfiles()}}.}

\item{seeds}{Character vector. Node ids to start the walks from; one
profile per seed.}

\item{diffusion_profile_params}{Named list. The PageRank parameters, see
\code{\link[=params_diffusion_profiles]{params_diffusion_profiles()}}.}

\item{.verbose}{Boolean. Controls verbosity. Defaults to \code{TRUE}.}
}
\value{
The \code{DiffusionProfiles} object with \code{profiles} populated as a
numeric matrix of nodes x seeds. Each column sums to 1.
}
\description{
Runs one constrained personalised PageRank per seed (Ruiz et al.). The graph
is built once in Rust and the seeds run in parallel. Each call replaces the
stored profiles, so pass drugs and diseases together if you want to compare
them afterwards.
}
