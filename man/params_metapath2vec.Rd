% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{params_metapath2vec}
\alias{params_metapath2vec}
\title{Wrapper function for the metapath2vec parameters}
\usage{
params_metapath2vec(
  walks_per_node = 40L,
  walk_length = 40L,
  n_epochs = 20L,
  n_negatives = 5L,
  window_size = 2L,
  lr = 0.01,
  sample = 0.001,
  num_workers = NULL
)
}
\arguments{
\item{walks_per_node}{Integer. Number of random walks per node of the
metapath's starting type. Defaults to \code{40L}.}

\item{walk_length}{Integer. Length of each random walk. Rounded up so the
walk closes on a full number of schema cycles. Defaults to \code{40L}.}

\item{n_epochs}{Integer. Number of training epochs. Defaults to \code{20L}.}

\item{n_negatives}{Integer. Number of negative samples. Defaults to \code{5L}.}

\item{window_size}{Integer. Context window size. Defaults to \code{2L}.}

\item{lr}{Numeric. Learning rate. Defaults to \code{1e-2}.}

\item{sample}{Numeric. Subsampling threshold for frequent nodes. Defaults to
\code{1e-3}.}

\item{num_workers}{Optional integer. If kept to \code{NULL}, it will default
to \verb{available cores - 2 (min 1)}.}
}
\value{
A list with the metapath2vec parameters.
}
\description{
Parameters for \code{\link[=metapath2vec]{metapath2vec()}}. There is no \code{p} or \code{q}: metapath walks are
first-order and follow the schema, not a biased return/explore rule.
}
