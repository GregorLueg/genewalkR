% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed_drift.R
\name{calculate_drift}
\alias{calculate_drift}
\title{Calculate embedding drift between two graphs}
\usage{
calculate_drift(object)
}
\arguments{
\item{object}{An \code{EmbedDrift} object, see \code{\link[=EmbedDrift]{EmbedDrift()}}.}
}
\value{
The \code{EmbedDrift} object with \code{aligned_embd} and \code{stats} populated.
\code{stats} is a \code{data.table} with columns \code{node}, \code{cosine_similarity}, and
\code{node_status} (\code{"shared"}, \code{"g1_only"}, \code{"g2_only"}).
}
\description{
Aligns the embedding of graph 2 onto the reference embedding of graph 1
via orthogonal Procrustes, then computes per-node cosine similarities as a
measure of context drift. Nodes shared between both graphs receive a cosine
similarity in \verb{[-1, 1]}. Nodes exclusive to graph 1 are assigned \code{-1.1} and
nodes exclusive to graph 2 are assigned \code{+1.1} as sentinel values indicating
absence rather than a computed similarity.
}
