% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metapath2vec.R
\name{metapath2vec}
\alias{metapath2vec}
\title{Metapath2Vec}
\usage{
metapath2vec(
  graph_dt,
  node_dt,
  metapath,
  embd_dim = 8L,
  metapath2vec_params = params_metapath2vec(),
  metapath_plus = FALSE,
  filter_unvisited = TRUE,
  directed = FALSE,
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{graph_dt}{data.table. The edge table. Needs to have the columns
\code{"from"} and \code{"to"}, and can optionally have a \code{"weight"} column.}

\item{node_dt}{data.table. The node table with the columns \code{"id"} and
\code{"type"}. Every edge endpoint needs to be in \code{id}.}

\item{metapath}{Character vector. The metapath over node types, closing on
its starting type, e.g. \code{c("gene", "pathway", "gene")}.}

\item{embd_dim}{Integer. Size of the embedding dimensions to create.
Defaults to \code{8L}.}

\item{metapath2vec_params}{Named list. The training parameters, see
\code{\link[=params_metapath2vec]{params_metapath2vec()}}.}

\item{metapath_plus}{Boolean. Use per-type negative sampling
(metapath2vec++). Defaults to \code{FALSE}.}

\item{filter_unvisited}{Boolean. Remove the rows of nodes that no walk
visited. Defaults to \code{TRUE}.}

\item{directed}{Boolean. Indicates if this is a directed or undirected
network. Defaults to \code{FALSE}.}

\item{seed}{Integer. Seed for reproducibility.}

\item{.verbose}{Boolean. Controls verbosity of the function.}
}
\value{
A numeric matrix of \verb{n_nodes x embd_dim} with the node ids as
rownames. Rows are ordered by node type. Carries three attributes:
\itemize{
\item node_type - Named factor with the type of each row.
\item unvisited_nodes - Character vector with the ids of the nodes no
walk visited. Removed from the matrix if \code{filter_unvisited = TRUE}.
\item walk_stats - List with \code{start_nodes}, \code{attempted}, \code{truncated},
\code{dropped}, \code{mean_length} and \code{walk_length}.
}
}
\description{
Runs metapath2vec over a heterogeneous graph. Random walks are constrained
to follow a metapath schema over node types, e.g.
\code{c("gene", "pathway", "gene")}, and a skip-gram model is trained on them.
With \code{metapath_plus = TRUE}, negative samples are drawn from the context
node's own type (metapath2vec++).

Walks start only on nodes of the metapath's first type. Nodes no walk ever
reaches keep their random initialisation, so by default they are removed
and listed in the \code{unvisited_nodes} attribute. Check the \code{walk_stats}
attribute as well: a high share of truncated or dropped walks means the
schema does not fit the graph.
}
\references{
Dong, Chawla and Swami, metapath2vec: Scalable Representation
Learning for Heterogeneous Networks, KDD 2017.
}
