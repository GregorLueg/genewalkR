% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diffusion.R
\name{diffuse_scores}
\alias{diffuse_scores}
\title{Run diffusion scoring}
\usage{
diffuse_scores(
  object,
  input,
  background = NULL,
  method = "raw",
  n_perm = 1000L,
  seed = 42L
)
}
\arguments{
\item{object}{A \code{DiffusionScores} object with a built kernel.}

\item{input}{Named numeric vector or named matrix. Names must correspond
to node names in the graph. For a vector, treated as a single input
column. For a matrix, rows are background nodes and columns are
separate input sets.}

\item{background}{Character vector. Node names forming the background
set. Defaults to all nodes in \code{input}.}

\item{method}{Character. One of \code{"raw"}, \code{"z"}, or \code{"mc"}. Defaults to
\code{"raw"}.}

\item{n_perm}{Integer. Number of permutations for \code{method = "mc"}.
Defaults to \code{1000L}.}

\item{seed}{Integer. RNG seed for \code{method = "mc"}. Defaults to \code{42L}.}
}
\value{
The \code{DiffusionScores} object with \code{scores} populated as a
\code{data.table} with columns \code{node} and one score column per input.
}
\description{
Diffuses input scores over the precomputed kernel. Requires
\code{build_kernel()} to have been called first.
}
