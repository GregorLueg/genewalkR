% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_metapath2vec}
\alias{rs_metapath2vec}
\title{Generate metapath2vec embeddings}
\usage{
rs_metapath2vec(
  node_ids,
  node_types,
  from,
  to,
  weights,
  metapath,
  metapath_plus,
  metapath2vec_params,
  embd_dim,
  directed,
  seed,
  verbose
)
}
\arguments{
\item{node_ids}{Character vector. Node identifiers.}

\item{node_types}{Character vector. Node type per node, same length as
\code{node_ids}.}

\item{from}{Integer vector. 1-based indices into \code{node_ids} for edge
origins.}

\item{to}{Integer vector. 1-based indices into \code{node_ids} for edge
destinations.}

\item{weights}{Optional numeric vector. Edge weights.}

\item{metapath}{String. Hyphen-separated metapath closing on its starting
type, e.g. \code{"gene-pathway-gene"}.}

\item{metapath_plus}{Boolean. Per-type negative sampling
(metapath2vec++).}

\item{metapath2vec_params}{Named list. Training parameters (walks_per_node,
walk_length, num_workers, n_epochs, n_negatives, window_size, lr,
sample).}

\item{embd_dim}{Integer. Embedding dimension.}

\item{directed}{Boolean. Treat graph as directed.}

\item{seed}{Integer. Random seed.}

\item{verbose}{Boolean. Controls verbosity.}
}
\value{
A list with:
\itemize{
\item embedding - Matrix of n_nodes x embd_dim, rows in \code{node_names}
order.
\item node_names - Node identifiers in row order.
\item node_types - Node type per row.
\item visited - Logical per row. \code{FALSE} if no surviving walk touched
the node, i.e. its row is the random initialisation.
\item walk_stats - Named list with the walk generation statistics.
}
}
\description{
Runs metapath-constrained random walks over a heterogeneous
graph and trains the skip-gram model on them (metapath2vec, or
metapath2vec++ with per-type negative sampling).
}
