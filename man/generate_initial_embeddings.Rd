% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed_drift.R
\name{generate_initial_embeddings}
\alias{generate_initial_embeddings}
\title{Generate initial node embeddings for both graphs}
\usage{
generate_initial_embeddings(
  object,
  embd_dim = 64L,
  node2vec_params = params_node2vec(),
  directed = FALSE,
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{An \code{EmbedDrift} object, see \code{\link[=EmbedDrift]{EmbedDrift()}}.}

\item{embd_dim}{Integer. Embedding dimension size. Defaults to \code{64L}.}

\item{node2vec_params}{Named list. Node2vec training parameters, see
\code{\link[=params_node2vec]{params_node2vec()}}.}

\item{directed}{Boolean. Whether to treat both graphs as directed. Defaults
to \code{FALSE}.}

\item{seed}{Integer. Random seed for reproducibility.}

\item{.verbose}{Boolean. Controls verbosity. Defaults to \code{TRUE}.}
}
\value{
The \code{EmbedDrift} object with \code{embd_1}, \code{embd_2}, and \code{params}
populated.
}
\description{
Runs node2vec independently on both graphs using identical hyperparameters,
storing the resulting embedding matrices in the object. Graph 1 serves as
the reference for subsequent Procrustes alignment. For details on the
node2vec algorithm, see Grover & Leskovec. For the approach generally
speaking, see Amir, et al.
}
\references{
Grover & Leskovec, arXiv, 2016 \cr
Amir, et al., Cancer Cell, 2025
}
