% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic_data.R
\name{differential_graph_test_data}
\alias{differential_graph_test_data}
\title{Generate Synthetic Differential Graph Data}
\usage{
differential_graph_test_data(
  n_stable = 50L,
  n_comm2 = 50L,
  n_comm3 = 50L,
  n_exclusive = 5L
)
}
\arguments{
\item{n_stable}{Integer. Number of nodes in the stable negative-control
community.}

\item{n_comm2}{Integer. Number of regular nodes in community 2, excluding
the hub.}

\item{n_comm3}{Integer. Number of nodes in community 3, excluding bridge
nodes.}

\item{n_exclusive}{Integer. Number of exclusive nodes per graph.}
}
\value{
A named list containing:
\itemize{
\item g1_edges - data.table with columns \code{from}, \code{to} for graph 1.
\item g1_nodes - data.table with columns \code{node}, \code{cluster} for graph 1.
\item g2_edges - data.table with columns \code{from}, \code{to} for graph 2.
\item g2_nodes - data.table with columns \code{node}, \code{cluster} for graph 2.
\item data_info - data.table with columns \code{node}, \code{is_differential},
\code{node_status} (\code{"shared"}, \code{"g1_only"}, \code{"g2_only"}).
}
}
\description{
Creates a pair of graphs with known topological differences to verify
context-aware differential embedding analysis. The two graphs share a common
backbone but differ in defined regions:

\itemize{
\item Community 1 is a stable negative control, identical across both
graphs.
\item Community 2 contains a hub node demoted to a peripheral node in
graph 2.
\item Two bridge nodes span communities 2 and 3 in graph 1 but are fully
embedded within community 3 in graph 2.
\item A set of exclusive nodes appears in only one graph.
}
}
